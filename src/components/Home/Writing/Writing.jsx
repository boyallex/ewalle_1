import React from "react";
import "../../../css/Writing.css"

const Writing = (props) => {
    return (
        <p className="head">
            {props.text}
        </p>
    )
};

export default Writing;